/**
 * {{description}}
 * @module {{name}}
 */
'use strict'

{{#each modules}}
import {{name}} from './{{path}}'
{{/each}}

export {
{{#each modules}}
  {{name}},
{{/each}}
}

{{#if defaultExport}}
export default {{defaultExport}}
{{/if}}