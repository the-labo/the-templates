/**
 * {{description}}
 * @module {{name}}
 */
'use strict'

{{#each modules}}
import {{name}} from './{{path}}'
{{/each}}

export {
{{#each modules}}
  {{name}}{{#if @last}}{{else}},
{{/if}}
{{/each}}

}
