/**
 * {{description}}
 * @module {{name}}
 */
'use strict'

{{#each modules}}
import {{name}} from './{{path}}'
{{/each}}

export {
{{#each modules}}
  {{name}},
{{/each}}
}

{{#if default}}
export default {{default}}
{{/if}}
