/**
* {{pascalcase name}} component
*/
'use strict'

import React from 'react'
import context from '../context'

class {{pascalcase name}} extends React.Component {
  #stateless = context.stateful()

  render () {
    return this.#stateless(
      ({ l, }) => {
        return (
          <div></div>
        )
      }
    )
  }
}


export default {{pascalcase name}}
