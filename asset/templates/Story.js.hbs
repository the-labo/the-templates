'use strict'

import c from './concerns'
import Story from './Story'
import {Urls} from '../../conf'

const {{name}}Base = c.compose(

)(Story)

class {{name}} extends {{name}}Base {
  async run () {
    await this.phase('__phase__', async ({ok}) => {
      ok(true)
    })
  }
}

module.exports = {{name}}

if (!module.parent) {
  require('../singleton').telStory(module.exports)
}
