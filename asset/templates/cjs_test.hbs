/**
 * Test for {{name}}.
 * Runs with mocha.
 */
'use strict'

const {{name}} = require('{{{relative}}}')
const { ok, equal } = require('assert')

describe('{{spinalcase name}}', () => {
  before(() => {
  })

  after(() => {
  })

  it('Do test', () => {

  })
})

/* global describe, before, after, it */
