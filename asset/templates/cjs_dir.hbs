/**
 * {{description}}
 * @module {{name}}
 */
'use strict'

const _d = (m) => 'default' in m ? m['default'] : m

{{#each modules}}
const {{name}} = _d(require('./{{path}}'))
{{/each}}

module.exports = {
{{#each modules}}
  {{name}},
{{/each}}
{{#if defaultExport}}
  default: {{defaultExport}},
{{/if}}
}
