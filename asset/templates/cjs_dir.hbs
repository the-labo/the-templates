/**
 * {{description}}
 * @module {{name}}
 */
'use strict'

{{#each modules}}
const {{name}} = require('./{{path}}')
{{/each}}

module.exports = {
{{#each modules}}
  {{name}},
{{/each}}
{{#if default}}
    default: {{defualt}}
{{/if}}
}
