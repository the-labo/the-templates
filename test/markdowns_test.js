/**
 * Test case for markdowns.
 * Runs with mocha.
 */
'use strict'

const markdowns = require('../lib/markdowns.js')
const assert = require('assert')
const co = require('co')

describe('markdowns', function () {
  this.timeout(3000)

  before(() => co(function * () {

  }))

  after(() => co(function * () {

  }))

  it('Markdowns', () => co(function * () {

  }))
})

/* global describe, before, after, it */
