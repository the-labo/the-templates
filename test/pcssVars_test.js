/**
 * Test case for pcssVars.
 * Runs with mocha.
 */
'use strict'

const pcssVars = require('../lib/pcssVars.js')
const assert = require('assert')
const co = require('co')

describe('pcss-vars', function () {
  this.timeout(3000)

  before(() => co(function * () {

  }))

  after(() => co(function * () {

  }))

  it('Pcss vars', () => co(function * () {

  }))
})

/* global describe, before, after, it */
