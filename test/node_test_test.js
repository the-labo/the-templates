/**
 * Test case for nodeTest.
 * Runs with mocha.
 */
'use strict'

const nodeTest = require('../lib/node_test.js')
const assert = require('assert')
const co = require('co')

describe('node-test', function () {
  this.timeout(3000)

  before(() => co(function * () {

  }))

  after(() => co(function * () {

  }))

  it('Node test', () => co(function * () {

  }))
})

/* global describe, before, after, it */
